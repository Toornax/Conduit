[package]
name = "conduit-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Conduit — moteur de graphe audio portable (types, tampons, graphe, DSP)"

[features]
default = ["serde"]
serde = ["dep:serde"]

[dependencies]
rtrb = "0.3"
serde = { workspace = true, optional = true }
thiserror = { workspace = true }

[dev-dependencies]
conduit-testing = { workspace = true }
proptest = { workspace = true }
serde_json = "1"

[lints]
workspace = true
