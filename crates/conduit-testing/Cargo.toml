[package]
name = "conduit-testing"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Conduit — utilitaires de test (allocateur de garde)"
publish = false

[dependencies]

# `tests/fuzz.rs` relit les manifestes des crates `crates/*/fuzz` (leurs `[[bin]]` et leur
# `[workspace]` vide) sans lancer cargo : c'est la moitié de la parade qui tourne partout,
# bac à sable Nix compris.
[dev-dependencies]
toml = "0.8"

[lints]
workspace = true
