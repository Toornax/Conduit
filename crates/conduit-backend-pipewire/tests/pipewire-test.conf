context.properties = { core.daemon = true, core.name = pipewire-0, support.dbus = false, default.clock.rate = 48000, default.clock.quantum = 256 }
context.spa-libs = { audio.convert.* = audioconvert/libspa-audioconvert, support.* = support/libspa-support }
context.modules = [
  { name = libpipewire-module-protocol-native }
  { name = libpipewire-module-access }
  { name = libpipewire-module-client-node }
  { name = libpipewire-module-adapter }
  { name = libpipewire-module-metadata }
  { name = libpipewire-module-link-factory }
  { name = libpipewire-module-spa-node-factory }
]
context.objects = [
  { factory = spa-node-factory args = { factory.name = support.node.driver node.name = Dummy-Driver node.group = pipewire.dummy priority.driver = 20000 } }
  { factory = adapter args = { factory.name = support.null-audio-sink node.name = "Null-Sink" media.class = "Audio/Sink" audio.position = "FL,FR" monitor.channel-volumes = true } }
]
